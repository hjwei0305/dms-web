import React from 'react';
import ExtTablePreview from '@/components/ExtTablePreview';

const Content = ({ tableUiConfig, store }) => {
  return <ExtTablePreview tableUiConfig={tableUiConfig} store={store} />;
};

export default Content;
